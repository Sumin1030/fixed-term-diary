function Label(prop) {
    /* prop
        title : 지역 이름
        Time: 시간
    */
    // const style = {};
    // if(prop.color) style.color = prop.color;
    // if(prop.fontSize) style.fontSize = prop.fontSize;
    // if(prop.fontWeight) style.fontWeight = prop.fontWeight;
    // if(prop.alignment) style.alignment = prop.alignment;
    
    // const className = prop.title+"-time";
    // return (
    //     <div className={className}>
    //         <div className="title">
    //             {prop.title.toUpperCase()}
    //         </div>
    //         <div className="today-time">
    //             {getDate(today_s)}
    //         </div>
    //     </div>
    // );
}

export default Label;